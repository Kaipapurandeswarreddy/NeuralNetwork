# NeuralNetwork
importing the libries that are use to implement the neural networks in pytorch
*First library is torch(syntax : import torch)
*Second importing nn module from the torch( syntax :import torch.nn as nn)
